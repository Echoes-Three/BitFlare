<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="BasicButton" TargetType="Button">
        <Setter Property="Foreground" Value="Blue"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- Basic Button -->
    <Style x:Key="ButtonHover" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="50"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontFamily" Value="{StaticResource Ibm Bios}"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="IsEnabled" Value="True"/>
        
        <!-- Remove default highlight and background on all buttons-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                </ControlTemplate>

            </Setter.Value>
        </Setter>
        
        <!-- Button hover -->
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource BlackBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Data Triggered integer switch -->
    <Style x:Key="IntegerSwitchButton" BasedOn="{StaticResource ButtonHover}" TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Width" Value="100"/>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsInteger}" Value="true">
                <Setter Property="Foreground" Value="{StaticResource BlackBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Data Triggered IEEE-754 switch -->
    <Style x:Key="Ieee754SwitchButton" BasedOn="{StaticResource ButtonHover}" TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Width" Value="100"/>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsIeee754}" Value="true">
                <Setter Property="Foreground" Value="{StaticResource BlackBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Data Triggered hexadecimal copy button hover -->
    <Style x:Key="HexadecimalCopyButtonHover" BasedOn="{StaticResource ButtonHover}" TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Width" Value="100"/>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding HexadecimalOutput}" Value="">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Foreground" Value="DimGray"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding HexadecimalOutput}" Value="{x:Null}">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Foreground" Value="DimGray"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Data Triggered output copy button hover -->
    <Style x:Key="OutputCopyButtonHover" BasedOn="{StaticResource ButtonHover}" TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Width" Value="100"/>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding BinaryOutput}" Value="">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Foreground" Value="DimGray"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding BinaryOutput}" Value="{x:Null}">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Foreground" Value="DimGray"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Data Triggered convert button hover -->
    <Style x:Key="ConvertButtonHover" BasedOn="{StaticResource ButtonHover}" TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Width" Value="100"/>
        
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsValidToConvert}" Value="False">
                <Setter Property="IsEnabled" Value="False"/>
                <Setter Property="Foreground" Value="DimGray"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>