<Window x:Class="BitFlare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BitFlare"
        mc:Ignorable="d" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="MainWindow" Height="350" Width="500">
    
    <Border Style="{DynamicResource MainWindowBorder}">
        
        <Grid>
            <!-- Separation of the main layout, two areas, title bar and body -->
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <!-- Title bar style -->
            <Border Grid.Row="0" Style="{StaticResource TitleBar}"/>
            
            <!-- Title bar logic -->
            <Grid Grid.Row="0">
                
                <!-- Title bar 3 sections to separate logo, draggable area, and title bar buttons -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20*"/>
                    <ColumnDefinition Width="60*"/>
                    <ColumnDefinition Width="20*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Draggable middle area -->
                <Border Grid.Column="1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Style="{StaticResource TitleBar}" Tag="DraggableArea"/>
                
                <!-- Left bolts and logo -->
                <Grid Grid.Column="0">
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*"/>
                        <ColumnDefinition Width="10*"/>
                        <ColumnDefinition Width="80*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="15*"/>
                        <RowDefinition Height="20*"/>
                        <RowDefinition Height="30*"/>
                        <RowDefinition Height="20*"/>
                        <RowDefinition Height="15*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Borders for better visualization -->
                    
                    <Border Grid.Column="0" Grid.Row="0" Background="LimeGreen" Opacity="0.5" />
                    <Border Grid.Column="0" Grid.Row="2" Background="LimeGreen" Opacity="0.5" />
                    <Border Grid.Column="0" Grid.Row="4" Background="LimeGreen" Opacity="0.5" />
                    
                    <Border Grid.Column="2" Grid.Row="0" Background="DeepPink" Opacity="0.5" />
                    <Border Grid.Column="2" Grid.Row="2" Background="DeepPink" Opacity="0.5" />
                    <Border Grid.Column="2" Grid.Row="4" Background="DeepPink" Opacity="0.5" />
                    
                    <Border Grid.Column="1" Grid.Row="1" Background="Orange" Opacity="0.5" />
                    <Border Grid.Column="1" Grid.Row="3" Background="Orange" Opacity="0.5" />
                    
                    <!-- Left bolts -->
                    <Ellipse Grid.Column="1" Grid.Row="1" Style="{StaticResource BoltsDesign}"/>
                    <Ellipse Grid.Column="1" Grid.Row="3" Style="{StaticResource BoltsDesign}"/>
                    
                    <!-- Working on -->
                    <ContentControl Grid.Column="2" Grid.Row="2" Content="{StaticResource Logo}" Width="50" Height="10" VerticalAlignment="Center" />
                
                </Grid>
                
                <!-- Right bolts and buttons -->
                <Grid Grid.Column="2">
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80*"/>
                        <ColumnDefinition Width="10*"/>
                        <ColumnDefinition Width="10*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="15*"/>
                        <RowDefinition Height="20*"/>
                        <RowDefinition Height="30*"/>
                        <RowDefinition Height="20*"/>
                        <RowDefinition Height="15*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Borders for better visualization -->
                    
                    <!--<Border Grid.Column="2" Grid.Row="0" Background="LimeGreen" Opacity="0.5" />
                    <Border Grid.Column="2" Grid.Row="2" Background="LimeGreen" Opacity="0.5" />
                    <Border Grid.Column="2" Grid.Row="4" Background="LimeGreen" Opacity="0.5" />
                    
                    <Border Grid.Column="0" Grid.Row="0" Background="DeepPink" Opacity="0.5" />
                    <Border Grid.Column="0" Grid.Row="2" Background="DeepPink" Opacity="0.5" />
                    <Border Grid.Column="0" Grid.Row="4" Background="DeepPink" Opacity="0.5" />
                    
                    <Border Grid.Column="1" Grid.Row="1" Background="Orange" Opacity="0.5" />
                    <Border Grid.Column="1" Grid.Row="3" Background="Orange" Opacity="0.5" />-->
                    
                    
                    <!-- Right bolts -->
                    <Ellipse Grid.Column="1" Grid.Row="1" Style="{StaticResource BoltsDesign}"/>
                    <Ellipse Grid.Column="1" Grid.Row="3" Style="{StaticResource BoltsDesign}"/>
                    
                    <!-- Title bar buttons grid -->
                    <Grid Grid.Column="0" Grid.Row="2">
                    
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="45*"/>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="45*"/>
                        </Grid.ColumnDefinitions>
                    
                        <Border Grid.Column="0" x:Name="MinimizeButtonContainer"  Style="{StaticResource MinimizeButtonHoverStyle}" Tag="IsMouseOver">
                            <Button x:Name="MinimizeButton" Click="Minimize_Click" Style="{StaticResource DefaultButtonStyle}"/>
                        </Border>
                        <Border  Grid.Column="2" x:Name="CloseButtonContainer" Style="{StaticResource CloseButtonHoverStyle}" Tag="IsMouseOver" >
                            <Button  x:Name="CloseButton"  Click="Close_Click" Style="{StaticResource DefaultButtonStyle}"/>
                        </Border>
                        
                        
                    </Grid>
                    
                </Grid>
                
                
            </Grid>
        </Grid>
    </Border>
</Window>
